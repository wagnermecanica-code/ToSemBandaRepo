ADD_VERY_GOOD_ANALYSIS_NOW.md
# ADICIONAR VERY_GOOD_ANALYSIS + LINT STRICT — EXECUTAR AGORA

Você está num projeto Flutter já 100% migrado para Clean Architecture + Riverpod + Freezed.

Execute imediatamente as seguintes ações com precisão cirúrgica:

1. Adicione a dependência mais recente de very_good_analysis como dev_dependency
2. Crie ou sobrescreva o arquivo analysis_options.yaml na raiz do projeto com exatamente este conteúdo (lint strict 2025):

```yaml
include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Regras extras que eu sempre uso em projetos elite (2025)
    always_specify_types: true
    prefer_single_quotes: true
    require_trailing_commas: true
    unnecessary_await_in_return: true
    unnecessary_lambdas: true
    prefer_relative_imports: true
    avoid_relative_lib_imports: false
    lines_longer_than_80_chars: false   # aceitável em Flutter
    avoid_print: true
    avoid_dynamic_calls: true
    use_build_context_synchronously: true
    use_key_in_widget_constructors: false